{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "library-rental-db (SQLite)",
            "database": "${workspaceFolder}/db/library.db"
        }
    ]
}
